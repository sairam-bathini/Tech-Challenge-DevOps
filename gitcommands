
Git commands

git init (create a local repo)

git status (to check the status of stagging area

git add . (to add the data from work directory to stagging area)

git commit -m "commit tagging name" (commiting data from stagging area to local)

git remote add origin <central git url> (to add the local repo code to github)

git push -m origin main/master (pushing data to main/master branch)

git log

git show

git branch (to check the existing branch)

git branch branch1 (creating new branch w.r.t existing branch)

git checkout branch1 (to change branch)

git branch -d branch1 (deleting the branch)

git merge <branch name> (merging data of branch name (branch) with current branch)

git stash

git stash list

git stash apply stash @{1}

git stash clear (clear stash memory)

git reset <filename> (UNDO before commit)

git revert <commit ID>

git clean -n (clean untracked files)

git clone

